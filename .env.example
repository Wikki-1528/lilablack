# AI Insights mode — set either key for live AI responses
# Without a key the app runs in Demo mode (pre-computed responses from real match data)
VITE_GROQ_API_KEY=your_groq_key_here
# VITE_GEMINI_API_KEY=your_gemini_key_here
